<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="AuraConfig.Pages.ThemesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:AuraConfig.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- ── LEFT: theme list ─────────────────────────────────────────── -->
        <Border Grid.Column="0"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ScrollViewer Grid.Row="0" Padding="8,8,8,0">
                    <StackPanel x:Name="ThemeListPanel" Spacing="2" />
                </ScrollViewer>

                <!-- New theme button -->
                <Button Grid.Row="1"
                        x:Name="NewThemeBtn"
                        Click="NewTheme_Click"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Left"
                        Margin="8,4,8,8"
                        Padding="12,8"
                        CornerRadius="6">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE710;" FontSize="14" />
                        <TextBlock Text="New theme" FontSize="13" />
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- ── RIGHT: editor panel ─────────────────────────────────────── -->
        <ScrollViewer Grid.Column="1" Padding="24,16,24,24">
            <StackPanel Spacing="16">

                <!-- Header -->
                <StackPanel Margin="0,0,0,4">
                    <TextBlock FontSize="40" FontWeight="SemiBold"
                               FontFamily="Segoe UI Variable Display"
                               Text="Themes" />
                    <TextBlock Style="{ThemeResource CaptionTextBlockStyle}"
                               Text="Create and manage your glow themes"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               Margin="0,2,0,0" />
                </StackPanel>

                <!-- No-selection placeholder -->
                <StackPanel x:Name="NoSelectionPanel"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Spacing="8"
                            Margin="0,60,0,0">
                    <FontIcon Glyph="&#xE790;" FontSize="48"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <TextBlock Text="Select a theme to edit"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center" />
                </StackPanel>

                <!-- Editor (hidden until a theme is selected) -->
                <StackPanel x:Name="EditorPanel" Visibility="Collapsed" Spacing="16">

                    <!-- Built-in notice -->
                    <Border x:Name="BuiltInNoticeBorder"
                            Background="{ThemeResource SystemFillColorCautionBackgroundBrush}"
                            CornerRadius="8" Padding="14,10" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE946;" FontSize="14"
                                      Foreground="{ThemeResource SystemFillColorCautionBrush}" />
                            <TextBlock Text="Built-in themes are read-only. Use Duplicate to create an editable copy."
                                       Style="{ThemeResource CaptionTextBlockStyle}"
                                       TextWrapping="Wrap"
                                       Foreground="{ThemeResource SystemFillColorCautionBrush}" />
                        </StackPanel>
                    </Border>

                    <!-- NAME card -->
                    <TextBlock Text="NAME"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                               CharacterSpacing="50" FontWeight="SemiBold" />

                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="12" Padding="20">
                        <StackPanel Spacing="8">
                            <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}"
                                       Text="Theme Name" />
                            <TextBox x:Name="NameInput"
                                     PlaceholderText="e.g. Midnight Blue"
                                     TextChanged="NameInput_TextChanged" />
                        </StackPanel>
                    </Border>

                    <!-- COLOUR card -->
                    <TextBlock Text="COLOUR"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                               CharacterSpacing="50" FontWeight="SemiBold"
                               Margin="0,8,0,0" />

                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="12" Padding="20">
                        <StackPanel Spacing="16">
                            <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}"
                                       Text="Glow Color" />
                            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,4,0,0">
                                <!-- Color preview swatch -->
                                <Border x:Name="ColorSwatch"
                                        Width="36" Height="36" CornerRadius="6"
                                        Background="#FF0078D4" />
                                <TextBox x:Name="ColorHexInput"
                                         PlaceholderText="#0078D4"
                                         Width="140"
                                         KeyDown="ColorHexInput_KeyDown" />
                                <Button Content="Apply color"
                                        Click="ApplyColor_Click" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- ANIMATION card -->
                    <TextBlock Text="ANIMATION"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                               CharacterSpacing="50" FontWeight="SemiBold"
                               Margin="0,8,0,0" />

                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="12" Padding="20">
                        <StackPanel Spacing="12">
                            <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}"
                                       Text="Glow Animation" />
                            <Slider x:Name="SpeedSlider"
                                    Minimum="0" Maximum="200" Value="100"
                                    Header="Pulse Speed"
                                    ValueChanged="SpeedSlider_ValueChanged" />
                        </StackPanel>
                    </Border>

                    <!-- OPTIONS card -->
                    <TextBlock Text="OPTIONS"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                               CharacterSpacing="50" FontWeight="SemiBold"
                               Margin="0,8,0,0" />

                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="12" Padding="20">
                        <StackPanel Spacing="16">
                            <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}"
                                       Text="Overlay Behavior" />
                            <ToggleSwitch x:Name="HoverToggle"
                                          Header="Show glow on hover"
                                          IsOn="True"
                                          Toggled="HoverToggle_Toggled" />
                            <ToggleSwitch x:Name="GlowToggle"
                                          Header="Enable glow effect"
                                          IsOn="True"
                                          Toggled="GlowToggle_Toggled" />
                        </StackPanel>
                    </Border>

                    <!-- AUDIO VISUALIZER card -->
                    <TextBlock Text="AUDIO VISUALIZER"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                               CharacterSpacing="50" FontWeight="SemiBold"
                               Margin="0,8,0,0" />

                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="12" Padding="20">
                        <StackPanel Spacing="12">
                            <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}"
                                       Text="Visualizer Bar" />
                            <ToggleSwitch x:Name="VisualizerToggle"
                                          Header="Show audio visualizer bar"
                                          IsOn="True"
                                          Toggled="VisualizerToggle_Toggled" />
                            <Slider x:Name="VisualizerHeightSlider"
                                    Minimum="40" Maximum="200" Value="80"
                                    Header="Bar height (px)"
                                    ValueChanged="VisualizerHeightSlider_ValueChanged" />
                            <Slider x:Name="VisualizerBrightnessSlider"
                                    Minimum="0" Maximum="200" Value="100"
                                    Header="Brightness (0–2×)"
                                    ValueChanged="VisualizerBrightnessSlider_ValueChanged" />
                        </StackPanel>
                    </Border>

                    <!-- PER-MONITOR GLOW card -->
                    <TextBlock Text="PER-MONITOR GLOW"
                               Style="{ThemeResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                               CharacterSpacing="50" FontWeight="SemiBold"
                               Margin="0,8,0,0" />

                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="12" Padding="20">
                        <StackPanel Spacing="16">
                            <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}"
                                       Text="Monitor Glow Overrides" />
                            <TextBlock Style="{ThemeResource CaptionTextBlockStyle}"
                                       Text="Leave color blank to inherit the global accent color."
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                       TextWrapping="Wrap" />

                            <!-- Monitor 1 — always visible -->
                            <StackPanel x:Name="MonitorRow0" Spacing="8">
                                <TextBlock Text="Monitor 1 — Primary" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <ToggleSwitch x:Name="Mon0Toggle"
                                              Header="Enable overlay on this monitor"
                                              IsOn="True"
                                              Toggled="Mon0Toggle_Toggled" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBox x:Name="Mon0HexInput"
                                             PlaceholderText="inherit global color"
                                             Width="180"
                                             KeyDown="Mon0Hex_KeyDown" />
                                    <Button Content="Apply color" Click="Mon0Apply_Click" />
                                </StackPanel>
                            </StackPanel>

                            <!-- Monitor 2 — shown when ≥ 2 monitors detected -->
                            <StackPanel x:Name="MonitorRow1" Spacing="8" Visibility="Collapsed">
                                <TextBlock Text="Monitor 2" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <ToggleSwitch x:Name="Mon1Toggle"
                                              Header="Enable overlay on this monitor"
                                              IsOn="True"
                                              Toggled="Mon1Toggle_Toggled" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBox x:Name="Mon1HexInput"
                                             PlaceholderText="inherit global color"
                                             Width="180"
                                             KeyDown="Mon1Hex_KeyDown" />
                                    <Button Content="Apply color" Click="Mon1Apply_Click" />
                                </StackPanel>
                            </StackPanel>

                            <!-- Monitor 3 -->
                            <StackPanel x:Name="MonitorRow2" Spacing="8" Visibility="Collapsed">
                                <TextBlock Text="Monitor 3" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <ToggleSwitch x:Name="Mon2Toggle"
                                              Header="Enable overlay on this monitor"
                                              IsOn="True"
                                              Toggled="Mon2Toggle_Toggled" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBox x:Name="Mon2HexInput"
                                             PlaceholderText="inherit global color"
                                             Width="180"
                                             KeyDown="Mon2Hex_KeyDown" />
                                    <Button Content="Apply color" Click="Mon2Apply_Click" />
                                </StackPanel>
                            </StackPanel>

                            <!-- Monitor 4 -->
                            <StackPanel x:Name="MonitorRow3" Spacing="8" Visibility="Collapsed">
                                <TextBlock Text="Monitor 4" FontSize="12"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <ToggleSwitch x:Name="Mon3Toggle"
                                              Header="Enable overlay on this monitor"
                                              IsOn="True"
                                              Toggled="Mon3Toggle_Toggled" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <TextBox x:Name="Mon3HexInput"
                                             PlaceholderText="inherit global color"
                                             Width="180"
                                             KeyDown="Mon3Hex_KeyDown" />
                                    <Button Content="Apply color" Click="Mon3Apply_Click" />
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Action buttons -->
                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                        <Button x:Name="ApplyBtn"
                                Click="Apply_Click"
                                Style="{ThemeResource AccentButtonStyle}"
                                Padding="16,8">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE73E;" FontSize="14" />
                                <TextBlock Text="Apply" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="DuplicateBtn"
                                Click="Duplicate_Click"
                                Padding="16,8">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE8C8;" FontSize="14" />
                                <TextBlock Text="Duplicate" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="SaveBtn"
                                Click="Save_Click"
                                Visibility="Collapsed"
                                Padding="16,8">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE74E;" FontSize="14" />
                                <TextBlock Text="Save" />
                            </StackPanel>
                        </Button>
                        <Button x:Name="DeleteBtn"
                                Click="Delete_Click"
                                Visibility="Collapsed"
                                Padding="16,8">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE74D;" FontSize="14" />
                                <TextBlock Text="Delete" />
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Status message -->
                    <Border x:Name="StatusBanner"
                            Background="{ThemeResource SystemFillColorSuccessBackgroundBrush}"
                            CornerRadius="8" Padding="14,10" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE73E;" FontSize="14"
                                      Foreground="{ThemeResource SystemFillColorSuccessBrush}" />
                            <TextBlock x:Name="StatusText"
                                       Style="{ThemeResource CaptionTextBlockStyle}"
                                       Foreground="{ThemeResource SystemFillColorSuccessBrush}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
